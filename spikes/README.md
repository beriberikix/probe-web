# Phase 0 spikes

Each spike has a literal exit criterion; "compiles" is not a result.

| Spike | Question | Result |
|---|---|---|
| A `rpc-ws` | Can `probe-rs-rpc-client` compiled to wasm32 talk to a native `probe-rs serve` over the browser WebSocket API? | **PASS** 2026-09-15. Chrome → `ws://127.0.0.1:3000/worker`, subprotocol `probe-rs.challenge-frame`, sha512 challenge handshake, `GetAllSchemas` compatible, `chips/list` = 266 families, 1055 ms end to end. Needs the patch in `../../prs-wasm-rpc` (worktree `wasm-rpc-client`, 6 files, +104/-31): tokio feature gating in `probe-rs-rpc`/`probe-rs-rpc-client`, wasm `WireSpawn`, timeout shim, byte-based uploads, and `serve.rs` sending the challenge as the first frame when the client offers the subprotocol. |
| B `webusb-worker` | Can the fork's async probe-rs + nusb WebUSB run inside a Web Worker, with postcard frames over `postMessage`? | **PASS** 2026-09-15 on FRDM-MCXA153 (MCU-Link CMSIS-DAP v2, SWD). Worker up in 32 ms, unmodified `probe-rs-rpc-client` over `postMessage`, schema check passed, `chips/list` = 204 families (fork), `probe/list` = 1, attach 36 ms, halt at pc=0x898, `memory/read32` @0x0 = [20006000, 00000e51, 000001e5, 00000dd3], resume; 599 ms end to end. Verified out-of-band: after navigating the tab away, native `probe-rs read --chip MCXA153 --protocol swd b32 0x0 4` returned the identical `20006000 00000e51 000001e5 00000dd3`. Finding: the worker's schema report advertises `master`'s full `ENDPOINT_LIST` although only 6 endpoints are dispatched, so `ensure_compatible` passes vacuously; `probe-web-local` must publish a truthful endpoint list (capability negotiation). |
| C `schema-ts` | Can the SDK's TypeScript types be generated from the RPC schema and match what crosses the wasm boundary? | **PASS** 2026-09-15. Instead of `tsify` on upstream types (orphan rule), `schema-ts` walks `probe-rs-rpc`'s postcard-schema model: 160 types, 66 endpoints, 5 topics → `probe-rs-rpc.d.ts` (1067 lines). A Node round-trip of `AttachRequest`/`AttachResult`/u64 through `serde-wasm-bindgen` is identical to the generated shapes under this contract: externally tagged enums, u64 as `bigint` (`serialize_large_number_types_as_bigints`), None/unit as `null` (`serialize_missing_as_null`), `Vec<u8>` as `Array<number>`. Byte-heavy endpoints (RTT, flash regions, temp-file append) will want a `Uint8Array` fast path in the SDK. |
| D perf | Is WebUSB throughput a problem, and would keep-alive endpoints help? | **PASS, question dissolved** 2026-09-15 on FRDM-MCXA153. Instrumented `usb_util` (fork, `stats` module) for a 4 KiB flash+verify: 2112 writes + 2112 reads, 54 342 bytes, **1.15 s in the browser (release wasm; 1.21 s debug) vs 0.99 s native**. Per transfer: submit→complete 225 µs, endpoint creation 1–3 µs. The 16 s figure from the earlier session was an artifact of per-transfer console tracing that was later stripped; endpoint keep-alive is not worth doing. Remaining ~15 % gap is Chrome's per-transfer round trip; the real lever, if ever needed, is fewer transfers (CMSIS-DAP block transfers), not the USB layer. Verified out-of-band: tag `SPIKE-D-REL-2133` written from the browser was read back natively at 0x1F000 after the tab released the device (`probe-rs read --chip MCXA153 --protocol swd b8 0x1F000 32`). |
| E `defmt-wasm` | Does `defmt-decoder` decode an RTT stream from ELF bytes in wasm, identical to native? | **PASS** 2026-09-15. Fixture: `defmt-fixture/` (thumbv7m, `defmt-semihosting`, run under `qemu-system-arm` lm3s6965evb → 56-byte stream, 5 frames incl. `{:?}` struct, `{:#x}` u64, f32, str, negative ints). `defmt-wasm/run.sh` decodes natively and in wasm (Node); outputs byte-identical, wasm decode 9.4 ms. `defmt-decoder` 1.1 compiles for wasm32 unchanged. |

Run A: `cd rpc-ws && ./build.sh`, start `probe-rs serve` from `rpc-ws/serve/` (config there: user `spike`, token `spike`), serve `www/` with `../serve_nocache.py` (port 8798), open `http://127.0.0.1:8798/?auto=1`.

## Phase 1 log

- 2026-09-15 — `@probe-web/client` + `@probe-web/devices` + `@probe-web/ui` (device-picker, flash-panel, rtt-terminal) + `apps/flash` written. `probe-web-core` (1.8 MB wasm) reuses the patched `probe-rs-rpc-client` unchanged; `probe-web-local` (9.4 MB wasm) implements 33 endpoints on the fork. Flasher over **WebSocket** to `probe-rs serve`: attach 52 ms, 4 KiB flash+verify 1.81 s, tag `PWEB-WS-2158` read back natively. Gotchas: Vite needs a tsconfig with `experimentalDecorators` in each Lit package (else "Unsupported decorator location: field"); `RttChannelConfig` is `serde(rename_all = "camelCase")` upstream but postcard-schema reports Rust field names, so the generated wire type is wrong for it (cast in the SDK; generator gap to fix).
- 2026-09-15 — RTT over WebSocket: `hardware-tests/firmware/mcxa153-rtt` (defmt-rtt, Cortex-M33) flashed from the flasher (8 KB, 3.0 s), RTT channel `defmt` discovered, 9 output events in 4 s. **Upstream bug found:** navigating the tab away mid-`monitor` makes `probe-rs serve` panic in `rpc/functions.rs:136` (`publish().await.unwrap()` on `ConnectionClosed`) and the probe lease is never released ("probe is in use" for every later client). Fixed in the `prs-wasm-rpc` worktree (break instead of unwrap); needs an upstream PR.
- 2026-09-15 — **RTT + in-browser defmt over WebSocket: PASS.** After fixing an auto-flow race (ELF set after the terminal started), the flasher flashed `mcxa153-rtt.elf` (8 KB, 3.9 s), attached RTT channel `defmt`, and decoded `hello from the FRDM-MCXA153`, `tick 0 (0x0)` … `tick 8 (0x8888)` in the browser from the ELF's defmt table; Stop → `UserExit`. Client-side: `Session.monitor` maps the server's post-cancel "channel closed" send error to `UserExit`.
- 2026-09-15 — Regression check of the serve fix (navigate away mid-`monitor`): the probe lease is now released (native read succeeds immediately after), but a second panic site remains — `rpc/functions/monitor.rs:52` unwraps the final `reply` to the vanished client. Same class of bug; fixing all reply/publish unwraps in spawned handlers.
- 2026-09-15 — Serve disconnect fix verified: navigating away mid-`monitor` now logs `client disconnected while publishing rtt` / `… before the MonitorEndpoint reply`, no panic, probe free immediately (native read succeeds). Patch in `prs-wasm-rpc` (`rpc/functions.rs`, `functions/{monitor,probe,test}.rs`), to go upstream with the wasm client PR.
- 2026-09-15 — **Capability negotiation** done: `RpcClient::negotiate()` (in `prs-wasm-rpc`) tolerates a server that serves a subset, errors only on key drift for shared endpoints, and returns `Capabilities`; `probe-web-local` advertises only its 33 implemented endpoints (`LOCAL_ENDPOINT_LIST`); `Client.capabilities()` / `Client.supports(path)` in the SDK. The flasher over WebUSB now logs 33 unsupported paths (all debug/test/core-register endpoints), exactly the matrix in the plan.
- 2026-09-15 (loop tick) — Generator gap closed: postcard-derive honors per-item `#[serde(rename)]` but not `rename_all`; the three upstream uses (`EspFlashMode`, `EspFlashFrequency`, `RttChannelConfig`) are listed in `schema-ts` and verified by a serde-wasm-bindgen round-trip under Node (`{"channelNumber":0,…}` MATCH, `"qio"`). The SDK cast is gone. Added `packages/client/test` (vitest). WebUSB grant still pending.
- 2026-09-15 (loop tick) — **Hardware-free local-transport test**: `probe-web-local --features fake` adds `probe_rs::integration::FakeProbe::with_mocked_core()` as a listed probe (VID:PID ffff:ffff); `scripts/build-wasm.sh` emits it under `packages/client/worker/fake/`, `Client.connect({ kind: 'webusb', fake: true })` selects it, and the flasher's `?fake=1` auto flow attaches to MCXA153 (71 ms), halts, reads 4 words, resumes → `FAKE_RESULT=PASS`. This is the Playwright/CI seed from the plan; the mocked core cannot run flash algorithms, so flashing stays hardware-only. WebUSB grant still pending.
- 2026-09-16 — **WebUSB flash through the in-tab worker: PASS** (4 KiB flash+verify 1.25 s vs 1.99 s over WebSocket; tag `PWEB-USB-0916A` read back natively at 0x1F000). RTT over the worker initially failed: the firmware went silent after "tick 0" with the PC inside the flash-algorithm blob (VTOR = 0x20002000). Root cause is in the fork branch: `nxp/sequences/mcx.rs::wait_for_stop_after_reset` spun while the AP *was* enabled and then ran the debug-mailbox handshake unconditionally (master only does so when the AP is down). Ported master's logic (fork commit pending). Also: the fork's `reset()` leaves the core `Halted(Request)` by design, so the worker now boots like the server (reset-and-halt → RTT attach → run). Separate fork bug noted: `TargetSelector::Auto` hangs on the MCXA153 (explicit chip works). Worker logs are now forwarded to the page as `log:` messages.
- 2026-09-16 — **RTT + in-browser defmt over WebUSB (in-tab worker): PASS**, two consecutive runs: RTT attached, decoded `hello from the FRDM-MCXA153`, `tick 0`…`tick 2`, Stop → `UserExit`. Root cause of the earlier silence was the fork's MCX debug-mailbox handshake: it slept 30 ms instead of waiting for the ROM's DMCSW/DMRETURN acknowledgements (master does), so after a reset the ROM stayed parked in its debug-session stub (PC in RAM, LR in ROM, R9 = 0xC33CA55A). Ported master's handshake + the AP-enabled check + an explicit flush after the AIRCR write into the fork (`nxp/sequences/mcx.rs`). Also confirmed: a Chrome tab holding the probe makes native tools fail with CMSIS-DAP "NoPacketSize"; that is exclusivity, not a wedge. RAM scans for the control block cost ~560 ms each over WebUSB; the SDK now takes the `_SEGGER_RTT` address from the ELF (`rttSymbolAddress`, via `object` in the core) for an exact region.
- 2026-09-16 — With the ELF-derived exact scan region, RTT attach over WebUSB takes **8.5 ms** (was ~560 ms per RAM scan) and the worker delivers **11 defmt events in 4 s** (`hello`, `tick 0`…, `five more ticks`), two identical runs. Phase 1's hardware matrix is now complete on the MCXA153 for both transports: flash (verified natively) and RTT with in-browser defmt decoding.
- 2026-09-16 — **ESP32-S3 over WebSocket: PASS.** Flasher with `esp-manifest.json` (bin at the spare region 0x7F0000, chip auto-detected as `esp32s3`, 2 cores, JTAG via the built-in USB-JTAG probe): attach 255 ms, 4 KiB flash+verify 1.69 s (64 KiB sector erase, compressed transfer), tag `PWEB-WS-ESP32S3-1` read back with `esptool.py --no-stub read-flash`. Added `?probe=<substring>` to the auto flow for multi-probe setups.
- 2026-09-16 — **ESP32-S3 over WebUSB (in-tab worker): PASS.** Auto-detected `esp32s3` (2 cores) through the worker, attach 599 ms, 4 KiB flash+verify **0.71 s** (vs 1.69 s via `probe-rs serve`), tag `PWEB-USB-ESP32S3-1` read back with `esptool.py --no-stub`. Hardware matrix status: MCXA153 (CMSIS-DAP v2, SWD) and ESP32-S3 (USB-JTAG, JTAG, Xtensa) verified on both transports; nRF9160/J-Link pending.
- 2026-09-16 — **nRF9160 (Thingy:91, J-Link, SWD) over WebSocket: PASS.** `nrf-manifest.json` (bin at 0xF0000): attach 82 ms, 4 KiB flash+verify 1.28 s, tag `PWEB-WS-NRF9160-1` read back natively.
- 2026-09-16 — **nRF9160 over WebUSB (in-tab worker): PASS.** Attach 125 ms, 4 KiB flash+verify 1.24 s, tag `PWEB-USB-NRF9160-1` read back natively.

### Phase 1 hardware matrix (complete)

| Board / probe / arch | WebUSB worker | WebSocket serve | Out-of-band check |
|---|---|---|---|
| FRDM-MCXA153, MCU-Link CMSIS-DAP v2, Cortex-M33 SWD | flash 1.25 s; RTT+defmt 11 events/4 s | flash 1.99 s; RTT+defmt 12 events/4 s | native probe-rs read |
| ESP32-S3 DevKitC, built-in USB-JTAG, Xtensa ×2 JTAG (auto-detect) | flash 0.71 s | flash 1.69 s | esptool `--no-stub` |
| Thingy:91 nRF9160, J-Link, Cortex-M33 SWD | flash 1.24 s | flash 1.28 s | native probe-rs read |
- 2026-09-16 — **Phase 1 close-out.** Worker instrumentation removed (one "RTT attached after …" line remains). SDK: `Client.close()`, per-probe Web Lock on the WebUSB transport (`probe-in-use-other-tab`); flash panel: *Verify only* and *Erase all*; app: `?op=verify|erase|cycle`. Exercised on the nRF9160 through the worker: `cycle` = flash → verify Ok → erase-all (~24 s, 256 sectors) → verify Mismatch → flash → verify Ok (**PASS**); a second tab attaching the same J-Link fails with `probe-in-use-other-tab` (**PASS**). Playwright (`npm run test:e2e`, `tests/fake-probe.spec.ts`): fake-probe local transport end to end + a UI guard, 2 passed in 2.8 s, no hardware. Not exercised: hotplug events (needs a physical unplug) and RTT down-channel input (test firmware has no down channel).
- 2026-09-16 — Hotplug verified: unplugging/replugging the J-Link with the flasher open logged `device disconnect: J-Link` / `device connect: J-Link` (WebUSB `connect`/`disconnect` events through `@probe-web/devices`), the grant survived, and the probe list refreshed. **Phase 1 closed.**
- 2026-09-16 — **RTT down channel: PASS over WebUSB.** New firmware `hardware-tests/firmware/nrf9160-rtt-echo` (`rtt-target` 0.6, String up channel + 64-byte down channel, echoes lines uppercased). Flasher `?send=hello` writes to down channel 0 once RTT is discovered (`Session.rttWrite`, the terminal's typed-input path): `sent 6 bytes`, output `echo: HELLO`. The terminal now takes a separate `elf` property for the `_SEGGER_RTT` scan region, independent of defmt decoding.
- 2026-09-16 — RTT down channel over WebSocket (`probe-rs serve`, `rtt/down`): same test, `echo: HELLO` received, PASS. Down-channel input is now covered on both transports; Phase 1 has no untested path left.

## Phase 2 log

Checklist (from the plan): [x] `@probe-web/artifacts` (File System Access handles, change polling → re-flash) · [x] `<probe-target-picker>` (search, chip info, YAML import) · [x] `apps/inspect` (`info` endpoint: DP/AP/ROM-table scan, chip identification; WebSocket only until the worker implements `info`) · [x] `<probe-semihosting-console>` (+ semihosting in the worker's monitor loop) · [x] `@probe-web/serial` + `<probe-serial-monitor>` · [x] Node usage of the SDK (WebSocket transport) · [x] `info` endpoint in the worker (implemented and CI-tested; hardware scan blocked by a fork bug, see log).
- 2026-09-16 — **`@probe-web/serial` + `<probe-serial-monitor>`: PASS (mocked tests and hardware).** Package: `requestPort` (filters for CP210x, CH34x, FTDI, Espressif, J-Link, Nordic, ST-Link, MCU-Link, DAPLink, Raspberry Pi; *any device* option), `grantedPorts`, `onPortsChanged`, `LineDecoder` (UTF-8 across chunks, CRLF), `SerialConnection` (8N1 defaults, read loop that survives non-fatal errors, `write` with line ending, `setSignals`, `resetViaRts` for ESP devkits, `closed` with a reason); own minimal WebSerial types, no new dependency. Component: baud picker, Choose port / Connect / Disconnect / Reset (RTS) / Clear, send line with ending, xterm output, `serial-line` and `serial-state` events; adopts a previously granted port. vitest: 2 new tests (decoder, loopback connection). Playwright: the flasher's section 6 against a mocked `navigator.serial` (choose port, 921600 baud, banner line, send `ping` → `PING` with CRLF written, disconnect) PASS. Firmware `hardware-tests/firmware/nrf9160-uart-echo` (raw UARTE0, P0.18/P0.19, banner, ticks, upper-cased line echo) verified natively on the Thingy:91's board-controller CDC port (`echo: NATIVE CHECK`). Browser run on the Thingy:91 (board controller 1915:9100, two CDC ports; the user granted both): in-page send `second run` → `echo: SECOND RUN` in 315 ms at 115200; after a reload, `/?idle=1&serialtest=after%20reload` adopted the remembered grant and passed in 316 ms (`SERIAL_RESULT=PASS`). Fix from the run: the monitor adopted a remembered port only when exactly one was granted, so a board exposing two UARTs got none after reload; it now takes the first and shows the count.
- 2026-09-16 — **`info` endpoint in the worker: implemented, CI PASS, hardware BLOCKED by a fork bug.** `crates/probe-web-local/src/info.rs` ports the fork's async scan (DP/AP walk, ROM tables, RISC-V/Xtensa checks) onto master's `probe_rs_rpc::info` wire types; the handler opens the probe without attaching a target and streams `TargetInfoDataTopic` events. Capability negotiation now enables Scan in `apps/inspect` on the local transport, and the Playwright test covers the request/topic round trip on the fake worker (the fake answers with one message event because `FakeProbe`'s DP register access is `todo!()`, and a wasm panic kills the worker). The flasher gained `?op=info` for hardware runs on the origin that holds the WebUSB grant.
  - Hardware: on the nRF9160 via J-Link over WebUSB the scan reads DPIDR, initializes the DP, lists 7 APs and reads AP0's ROM table base (0xE00FF000); the first ROM table entry read then never completes and the J-Link re-enumerates. **The same hang reproduces natively** with the fork (`probe-rs/probe-rs/examples/e2e_romtable.rs`, uncommitted: last trace line is the TAR write for 0xE00FF000), while master's scan through `probe-rs serve` works. So it is a fork `probe-rs` bug, not WebUSB and not the port. Master has many J-Link/SWD/queue changes since the fork base (e.g. #3828 queue refactoring, #3932, #4334, #4348); bisecting them is left for Phase 4 (rebase onto master). The probe and board were healthy afterwards (Node HIL run PASS). A CMSIS-DAP or ESP board would show whether the bug is J-Link-specific.
- 2026-09-16 — **Node usage of the SDK: PASS** (`examples/node-ci/run.ts`, nRF9160 through `probe-rs serve`). A hardware-in-the-loop runner: flash with verify, independent verify, monitor RTT and semihosting, `--expect` strings, exit code from the result. Semihosting firmware: flashed + verified in 2.6 s, all output lines, `SemihostingExit(Ok)` at 5.5 s, exit 0. RTT echo firmware: banner and ticks, exit 0; with a missing `--expect` it exits 1. Changes needed in `@probe-web/client`: `ensureWasm` reads the `.wasm` from disk under Node (Node's `fetch` rejects `file:` URLs), and the source avoids parameter properties because Node runs TypeScript with type stripping only; `erasableSyntaxOnly` in the client tsconfig keeps it that way. Node 26 dropped `--experimental-transform-types`, so plain `node run.ts` is the supported invocation. The browser apps and Playwright suite are unaffected.
- 2026-09-16 — **`<probe-semihosting-console>`: PASS on both transports** (nRF9160, `hardware-tests/firmware/nrf9160-semihosting`: banner, three stdout lines, one stderr line, `SYS_EXIT` success). The console listens to the RTT terminal's `monitor-event`/`monitor-exit` events and renders stdout, stderr and the exit status. WebSocket: 12 semihosting events in 2.0 s, `SemihostingExit(Ok)`. WebUSB: the worker's monitor loop gained console-only semihosting (`crates/probe-web-local/src/semihosting.rs`: `:tt` opens map to stdout/stderr, writes publish on `SemihostingTopic`, exit codes end the monitor, file operations are refused because a browser has no host filesystem); 12 events in 2.2 s, `SemihostingExit(Ok)`. RTT echo re-verified over WebUSB afterwards.
  - Finding: with an RTT client whose scan region is all of RAM, serve's run loop rescans RAM on every poll and semihosting output never arrived within 17 s (native `probe-rs run` through the same serve worked). Fix in the SDK: `<probe-rtt-terminal>` creates no RTT client when the ELF has no `_SEGGER_RTT` symbol (`elfHasRtt`, `Session.clearRttClient`).
  - Serve bug found and patched in `prs-wasm-rpc` (uncommitted): `monitor.rs` unwrapped the semihosting send, so a client leaving mid-monitor panicked the worker thread and the `run_blocking` join; it now cancels the run loop instead.
  - Flash panel: a file handle remembered from an earlier visit no longer silently replaces the manifest image when its permission has lapsed (that failed the auto flow with "User activation is required"); it shows *Resume watching* instead, and resumes silently only when read permission is still granted.
- 2026-09-16 — **Watch-and-reflash: PASS** (the plan's headline Phase 2 feature). `@probe-web/artifacts`: `pickFile()` (File System Access), handles persisted in IndexedDB (`rememberHandle`/`recallHandle`), `watchFile()` polling `lastModified`/`size` with a settle debounce (unit-tested with fake timers), `fromFile`/`fromUrl`. Flash panel: *Pick file & watch…*, *re-flash on change*; the app restarts the RTT monitor after a re-flash. Demo on the nRF9160 over WebSocket: picked `nrf9160-rtt-echo.elf` once; rebuilding the firmware with a new banner and copying it over the file triggered `watched file changed` in the same second, a 2.56 s re-flash with no click, and the terminal showed the new banner ticking from 0 after the old build's tick 54.
- 2026-09-16 — **`<probe-target-picker>`: PASS** (Playwright, fake worker). Searches `chips/list` (families × variants), `chips/info` renders cores and the memory map, *import target YAML* calls `chips/load` (fixture: the MCXA family renamed, 16 variants) and the new chips become searchable. Wired into the flasher (selection fills the chip field).
- 2026-09-16 — Generator gap noted: recursive wire types (`ComponentTreeNode.children: Vec<ComponentTreeNode>`) come out as `Array<null>` in `wire.ts` because probe-rs-rpc's hand-written `Schema` impl uses `Vec<()>` as a placeholder (postcard-schema cannot express recursion). Fixed 2026-09-17 with a `RECURSIVE` table in the generator: `children: Array<ComponentTreeNode>`.
- 2026-09-16 — **`apps/inspect`: PASS over WebSocket** (port 5174). `Client.info()` streams `InfoEvent`s; the page renders them as a foldable tree. nRF9160 via J-Link, SWD: DPv2 (designer 0x4:0x3b), 7 APs, MemoryAP 0 → Cortex-M33 ROM table (CPUID, DWT, FPB, ITM, ETM, CTI), MemoryAP 3 → Nordic ROM table (ETB, TPIU, TSGEN, CTIs, trace funnels), AP 1 inaccessible (DeviceEn clear), 3 unknown APs; multidrop DPs and RISC-V/Xtensa reported as not applicable. At the time the worker had no `info` endpoint and the app disabled Scan (`Client.supports('info')`); the worker implements it since, see below.

## Bug-fix pass (2026-09-17)

Addressed the known bugs and issues from Phases 0–2 before Phase 3. Fork changes are in `probe-rs` (branch `webusb/nusb-0.2.7`), uncommitted.

- **Fork ROM-table hang: FIXED.** Root cause: `architecture/arm/memory/romtable.rs` implemented `Stream for RomTableIterator` by hand; `poll_next` polled the `read_32` future once and dropped it on `Pending`, abandoning the USB transfer (nothing woke the task; the J-Link was left mid-command and re-enumerated). Replaced with a plain async loop. Native `examples/e2e_romtable.rs` now parses AP0's ROM table in ~2 s. Over WebUSB: flasher `?op=info` → 1 DP, 7 APs, 26 component nodes in 10.5 s (`INFO_RESULT=PASS`); the inspect app shows the tree.
- **Fork auto-detect hang: FIXED (same bug), plus two bugs it had been hiding.** With the hang gone, auto-detect on the nRF9160 picked `RP235x` (the fork's registry matches any Arm-designed Cortex-M33 ROM table, part 0x4c9, to it) and then panicked on an ADIv6 AP address against an ADIv5 DP (`unreachable!` in `select_ap_and_ap_bank`). Fixes: that `unreachable!` returns `ArmError::WrongApVersion`; the generic registry lookup no longer guesses a target from an Arm-designed ROM table; master's RP235x chip-ID detection hook is ported (`vendor/raspberrypi`, detection only); `try_detect_arm_chip` closes the interface before propagating errors; RISC-V/Xtensa detection checks for the interface first and logs instead of failing detection (as master). Native `examples/e2e_autodetect.rs` on the nRF9160: clean "unable to load specification for chip" in 139 ms (master cannot auto-detect it either: its first ROM table is Arm's). MCXA153 (MCU-Link CMSIS-DAP): native auto-detect no longer hangs and fails cleanly in 64 ms (its ROM table reports NXP with PART 0, which cannot identify a chip, so no `part:` was added to the YAML); native ROM-table parse works.
- **Fork wasm hazards: FIXED.** 48 `thread::sleep` → `usb_util::wait(..).await` (all were in async fns), 7 `std::time::Instant` imports → `web_time`. `clippy.toml` now bans `std::thread::sleep`, `Instant::now`, `SystemTime::now` and the std `Instant` type; the ban is enforced for wasm32 (`cargo clippy -p probe-rs --target wasm32-unknown-unknown --no-default-features --features builtin-targets`, with `--cfg=web_sys_unstable_apis`) and allowed natively, where `web_time::Instant` is the std type. Clean, and a planted violation is caught.
- **Timeouts as control flow (WebUSB cannot cancel a transfer):** ESP USB-JTAG no longer drains by reading until a timeout on wasm (resynchronises with clocked bits instead) and its descriptor retry waits between attempts; J-Link `read` gives up after repeated zero-length reads; USB timeouts on wasm say the probe must be reopened. ST-Link (no USB reset-and-retry on wasm) and WCH-Link (1 s per-command timeout on wasm) changes are **hardware-unverified**. SWO readers still use timeouts; probe-web has no SWO feature (known limitation).
- **`FakeProbe`:** DP/AP register access and `flush` return `NotImplemented`/`Ok` instead of `todo!()`, so the worker's fake scan runs (5 events) instead of panicking.
- **Worker crash no longer hangs the page.** The worker posts `fatal:<reason>` from a panic hook, when its server stops, and from `error`/`unhandledrejection`/failed start in `local-worker.js`. The transport closes on `fatal:`, `onerror` or `onmessageerror` (pending calls fail), accepts only the exact `ready`, and times out start-up (30 s) and the schema handshake (15 s). The SDK reports such failures as `kind: 'worker-crashed'` with the reason (`Client.crashReason`). Playwright `worker-crash.spec.ts` (fake build panics on a scan of probe ffff:fffe): in-flight and later calls reject as `worker-crashed` — PASS.
- **Worker gaps:** `info` honours an explicit `scan_chain` (sets IR lengths on JTAG probes, reports when the probe has none); RTT remembers where the control block was found, so down-channel writes and channel queries no longer rescan RAM each time; `keep_control_block` follows whether the image writes the block; RTT channel-mode failures are logged; the monitor stops when publishing to a disconnected client fails; an `expect` in semihosting removed; unused-import warnings gone.
- **Inspect app shares the flasher's WebUSB grant:** the flasher's dev server also serves it at `/inspect/` (grants are per origin; a deployment hosts both on one site). Playwright uses `/inspect/`.
- **Regression on the nRF9160/J-Link:** WebUSB flash + RTT down-channel echo (`echo: BUGFIX-PASS`) PASS, WebUSB semihosting (12 events, `SemihostingExit(Ok)`, flash 1.84 s) PASS, Node runner over WebSocket PASS. Fork unit tests 147/147. vitest 4/4, Playwright 6/6. One WebUSB attach right after leaving the inspect page failed with a probe error and passed on retry (the previous page's worker was still releasing the device).
- **MCXA153 over WebUSB (CMSIS-DAP):** flash + defmt RTT PASS (flash 2.15 s, RTT attached after 454 ms, 14 events in 5 s); `?op=info` → 1 DP, 2 APs, 13 component nodes (`INFO_RESULT=PASS`). ESP32-S3 (built-in USB-JTAG, JTAG): native fork auto-detect → `esp32s3` in 417 ms; WebUSB flash with an empty chip (auto-detect in the worker, with the new ESP flush handling) → `esp32s3`, 1.41 s, and an independent esptool `--no-stub read-flash 0x7F0000 32` returned the tag `BUGFIX-ESP-0917`. The first WebUSB attempt, started as the board was being plugged in, saw the device re-enumerating and found no probe; the retry passed and the device stays enumerated when idle.

## Phase 3 log

Plan: `~/.claude/plans/i-have-attached-an-snug-donut.md` ("Current task: Phase 3"). Debugging goes over the WebSocket transport to `probe-rs serve` (the WebUSB worker has no debug endpoints).

Slices: [x] 1 firmware + `Debugger` run control/status/registers/memory · [x] 2 debug info, stack trace, scopes/variables/evaluate/set_variable, SVD · [x] 3 breakpoints, stepping, disassembly, source mapping · [x] 4 components A (core-controls, registers, callstack, variables) · [x] 5 components B (breakpoints, disassembly, memory-view, peripherals) · [x] 6 `@probe-web/dap` · [x] 7 `apps/workbench` · [x] 8 `examples/monaco-ide` + hardware matrix. **Phase 3 complete.**

- 2026-09-17 — **Slice 1: PASS on the FRDM-MCXA153 (MCU-Link, CMSIS-DAP) over WebSocket.**
  - Firmware `hardware-tests/firmware/cm33-debug` (features `mcxa153` / `nrf9160`; opt-level 1, full DWARF): `main → step_a(n) → step_b(point, mode)` with exact, documented values; statics `COUNTER`, `TABLE`, `GREETING`; RTT banner. Built to `apps/flash/public/firmware/{mcxa153,nrf9160}-debug.elf`.
  - `probe-web-core` wraps every debug endpoint the client offers: session (`loadDebugInfo`, `loadSvd`/`clearSvd`, `richStackTrace`, `scopes`, `variables`, `evaluate`, `setVariable`, `clearCoreDebugState`, `step`, `resolveSourceBreakpoints`/`Locations`, `disassemble`, `haltCores`/`resumeCores`/`coresStatus`) and core (`readRegisters`, `writeRegister`, `set/clearHwBreakpoints`, `enableVectorCatch`, `metadata`, `readBytes`, memory 16/64). Plus `elfSymbolAddress`.
  - `scripts/gen-registers.py` generates `packages/client/src/registers.generated.ts` from probe-rs master's register tables (Arm common, Cortex-M, FP, ARMv8-M main/security, RISC-V, Xtensa; two Xtensa FP registers with computed ids skipped), because the RPC names registers only by id.
  - `packages/client/src/debugger.ts`: `session.debugger({ core })` → serialised calls; status poller emitting `stopped {reason, pc}` / `continued` / `locked-up` on transitions; `pause`, `continue` and `step(instruction|over|into|out)` (clearing server debug state and bumping an epoch before the core moves), `reset`, `resetAndHalt`, vector catch; register table per core type (FP registers limited to the reported count), `readRegisters`, `writeRegister(name|id)`, `readMemory`/`writeMemory`. vitest: 5 tests against a scripted fake session (transitions, call order, serialisation, registers, FP filtering) PASS.
  - `examples/node-ci/debug.ts`: hardware test for the above (pause, PC/SP/xPSR, TABLE bytes, COUNTER advancing, register and memory write round trips, instruction step, reset-and-halt at the reset vector, poller seeing an external halt).
  - Hardware (`node examples/node-ci/debug.ts --elf …/mcxa153-debug.elf --chip MCXA153 --probe mcu-link`, `DEBUG_RESULT=PASS`, 7.7 s incl. flashing): pause → `Halted(Request)` at pc 0x900 with a `stopped` event; 30 registers readable (R0–R15, MSP, PSP, EXTRA, XPSR and the ARMv8-M main/security extras; the MCXA153 has no FPU); SP 0x20005fc8, xPSR Thumb bit set; `TABLE` bytes 11 11 22 22 33 33 44 44; `COUNTER` advanced 4 → 6 over 1.2 s; R12 and `COUNTER` write/read round trips; instruction step 0x900 → 0x902; reset-and-halt stops at the reset handler (0x7c0, vector 0x7c1); the poller reported a halt made outside the `Debugger`.
  - Node runs the client TypeScript with type stripping only, which needs explicit `.ts` extensions on runtime imports; every tsconfig now sets `allowImportingTsExtensions`. vitest 9/9, Playwright 6/6.
- 2026-09-17 — **Slice 2: PASS on the FRDM-MCXA153 over WebSocket** (`DEBUG_RESULT=PASS`, all 27 checks of slices 1–2).
  - `Debugger`: `loadDebugInfo(elf)` (upload + DWARF), `loadSvd`/`clearSvd`, `stackTrace()` (one `stack_trace/rich` per stop, cached), `scopes(frameId)`, `variables(ref, filter?)`, `evaluate(name, frameId?)`, `setVariable(variable, value)`. Friendly types (`Frame`, `Scope`, `Variable` with its `parent` key, `Evaluation`); u64 references become numbers.
  - Handle safety: every frame id and variables reference handed out is recorded for the current stop; anything else is refused with `kind: 'stale-reference'` before it reaches the server (which would panic the connection). Resuming, stepping, resets, and a resume observed by the poller all start a new epoch. Also `kind: 'no-debug-info'` and `'not-halted'`.
  - Hardware, breakpoint on `step_b` (raw hw breakpoint; wrapped in slice 3): stop reported as `Breakpoint` at the function address; stack `step_b ← step_a ← __cortex_m_rt_main ← trampoline ← Reset` with `main.rs:39`; `point == {n, 2n}` and the `Counting` variant; statics `COUNTER == n − 1` and `TABLE` elements; `evaluate` of `COUNTER` and `R0`; `setVariable` of `COUNTER` changed what the firmware computed on the next hit (100 → 101); a pre-resume reference was refused afterwards; the SVD Peripherals scope (test SVD `hardware-tests/svd/cortex-m-scb.svd`) read `SCB.CPUID` = 0x411FD210, matching memory, with `IMPLEMENTER` = Arm.
  - Limitation found: writing a local of an unwound caller frame in optimised code (main's `n`) reads back but does not affect the firmware, because its DWARF location is a stack slot while the loop keeps the value in a register. `step_a`'s locals are optimised out at opt-level 1.
  - vitest now scoped by a root `vitest.config.ts` (it was also picking up the Playwright specs): 10/10; Playwright 6/6.
- 2026-09-17 — **Slice 3: PASS on the FRDM-MCXA153 over WebSocket** (`DEBUG_RESULT=PASS`, 41 checks across slices 1–3, 12.3 s incl. flashing).
  - `Debugger` breakpoints: `setSourceBreakpoints(path, [{line, column?}])` replaces one file's breakpoints (DAP-style; a relative suffix like `src/main.rs` matches the DWARF path), `setInstructionBreakpoints(addresses)`, `clearBreakpoints()`, `reapplyBreakpoints()` (also run automatically after loading a new ELF). Each `Breakpoint` reports `verified`, the placed `address`, the resolved `source` (probe-rs may move a line) and a `message` (no code there, or no free comparator). Comparators are reference-counted per address, so a source and an instruction breakpoint on the same address share one, and a comparator is cleared only when nothing uses it. `stopped` now names the breakpoints at the stop PC.
  - `disassemble(address, count, instructionOffset?, byteOffset?)` → `Instruction {address, text, bytes, source}`; `resolveSourceLocations(addresses)`.
  - `packages/client/src/sources.ts`: `matchSourcePath` (longest `/`-boundary suffix, Windows separators), `DirectorySourceProvider` (a picked directory, indexed once, skipping `target/`, `node_modules/`, dot-dirs), `UrlSourceProvider` (build prefix → base URL). vitest 14/14 (source matching, providers, breakpoint sharing/budget/replacement).
  - Hardware: a source breakpoint on `let sum = …` verified at line 40 (0x992) and hit with its id in the stop; step over stayed in `step_b` (the top frame was `i32::wrapping_mul`, inlined, which probe-rs reports as its own frame); a breakpoint at the `step_b(...)` call site in `step_a` hit at line 57; step into → `step_b`, step out → `step_a:58`; an instruction breakpoint on `step_a` resolved to `main.rs:54` and hit at 0x938; disassembly from the PC (`push {r7, lr}`, …) with source lines, and with offset −2 (two instructions before, then the PC); `resolveSourceLocations(step_b)` → `main.rs:39`; asking for 10 hardware breakpoints verified 4 (the Cortex-M33's FPB) and reported "No available hardware breakpoints" on the rest; with all cleared the core keeps running.
  - Note for UIs: stepping frequently stops in code inlined from `core`; show inlined frames, but pick the first non-inlined frame for "current function".
- 2026-09-17 — **Slice 4: PASS** (Playwright with a fake debugger; hardware in Chrome on the FRDM-MCXA153 over WebSocket).
  - Components in `@probe-web/ui`, each driven only by a `debugger` property and its events: `<probe-core-controls>` (continue, pause, step over/into/out/instruction, reset, reset + halt, vector catch; state line such as "halted — breakpoint 1 at 0x00000992"), `<probe-registers>` (core / system / floating point groups, FP collapsed by default, role labels like `R13 (SP)`, changed-since-last-stop highlight, stale while running, double-click to edit), `<probe-callstack>` (inlined frames marked, the first real frame selected by default, `frame-selected` event), `<probe-variables>` (scopes with lazy children, expansion kept across stops by path, double-click to edit leaves via `setVariable`, watch expressions re-evaluated at each stop). Shared styles in `debug-style.ts`.
  - `@probe-web/client/testing` exports `FakeDebugger`: a scripted `main → step_a → step_b` program with registers, scopes, run/pause/step, breakpoints and disassembly, recording every mutating call. Reused by later slices.
  - Test page `apps/flash/debug.html` (`?fake=1`, or `?auto=1` for a real target). Playwright `tests/debug-components.spec.ts`: 4 tests (stop rendering and frame selection; controls and stale/refresh with change marks; register and variable edits reaching the debugger; expansion kept and watches re-evaluated on a new stop) PASS. Suite 10/10.
  - Hardware (`debug.html?auto=1&token=spike&probe=mcu&chip=MCXA153&elf=/firmware/mcxa153-debug.elf&line=40`, `DEBUGUI_RESULT=PASS` in 8.1 s incl. flashing): breakpoint at main.rs:40 hit; call stack `step_b ← step_a ← __cortex_m_rt_main …` with `step_b` selected; `point` expanded through the UI to `x = 1`; R15 shows the stop PC 0x992; the controls read "halted — breakpoint 1"; clicking Step over moved R15 to 0x994 with the change mark. After the step the inlined `wrapping_mul` frame is on top and `point.x` reads "optimized away" (opt-level 1), as expected.
- 2026-09-17 — **Slice 5: PASS** (Playwright with the fake debugger; hardware in Chrome on the FRDM-MCXA153 over WebSocket).
  - Components (on a small `DebuggerElement` base that subscribes to `stopped` / `continued` / `breakpoints`): `<probe-breakpoints>` (add `file:line` or an address, verified dot or the failure message, placed address and moved line, hit highlight, remove one or all), `<probe-disassembly>` (instructions around the PC or a chosen address, PC row, source headers, gutter click toggles an instruction breakpoint, Follow PC), `<probe-memory-view>` (hex + ASCII, 1/2/4/8-byte groups, little/big endian, refresh on stop or manual, changed values marked, byte edit, Intel HEX export; `intel-hex.ts` with unit tests), `<probe-peripherals>` (load an SVD from a file or bytes, peripherals → registers → fields tree, name filter, refreshed at stops).
  - `FakeDebugger` gained a Peripherals scope after `loadSvd`. Playwright `tests/debug-components-b.spec.ts`: 4 tests PASS; suite 14/14; vitest 17/17.
  - Hardware (`debug.html?auto=1&…&table=0x1c98&svd=/svd/cortex-m-scb.svd`, `DEBUGUI_RESULT=PASS`, 17.4 s): breakpoints panel "● main.rs:40 0x00000992"; disassembly PC row 0x994 `add.w r0, r0, r0, lsl #1`; gutter click set a verified instruction breakpoint on the next row (0x998); memory view at `TABLE` showed `11 11 22 22 33 33 44 44`, as u16 `1111 2222 3333 4444`, and exported `:101C9800111122223333…`; peripherals panel read `SCB.CPUID` = 0x411FD210. Slice 4's checks passed again in the same run.
  - Found: editing the test page made Vite reload a tab that was mid-run; the WebSocket connections from that page stayed established on `probe-rs serve` (which kept retrying a failing AP write), holding the probe ("probe is in use") until serve was restarted. The test page now closes its client on `pagehide`; apps should do the same.
- 2026-09-17 — **Slice 6: `@probe-web/dap` PASS** (vitest with the fake debugger; hardware in Node on the FRDM-MCXA153 over WebSocket).
  - `ProbeDebugAdapter`: a hand-written DAP loop typed with `@vscode/debugprotocol` (types only), in the shape VS Code web's inline adapters use (`handleMessage` / `onDidSendMessage`). Requests: initialize, launch/attach (custom args: `url`, `token`, `probe`, `chip`, `protocol`, `program` bytes or URL, `flash`, `svd`, `stopOnEntry`), setBreakpoints, setInstructionBreakpoints, configurationDone, threads (one, id 1), stackTrace (paged; inlined frames `subtle`; `instructionPointerReference`), scopes (locals/registers hints), variables, setVariable (Registers scope → `writeRegister`, else `setVariable`), evaluate, source (via a `SourceProvider`), continue, pause, next/stepIn/stepOut (with `granularity: instruction`), readMemory/writeMemory (base64, `memory` event), disassemble, disconnect/terminate. Events: initialized, stopped (entry/breakpoint with `hitBreakpointIds`/step/pause/exception), continued, output, memory, terminated. Every handler answers; failures become `success: false` with the message. `connectProbeRs` is the default connection (WebSocket, attach, flash on launch, debug info, SVD); hosts can inject their own `connect`.
  - Found on hardware: resetting the MCXA153 clears the hardware breakpoints, so `launch` now resets before sending `initialized` (as probe-rs's DAP server does) and `configurationDone` only runs or reports the entry stop. A statement step (step out) that started on one of our own hardware breakpoints stopped there without moving; `Debugger.step` now lifts that breakpoint for the step and restores it (unit test added). Stepping out from line 40 of `step_b` in the opt-level-1 firmware runs on into `main` (from line 43 it lands in `step_a`): probe-rs stepping behaviour in optimised code, not the adapter.
  - vitest `packages/dap/test/adapter.test.ts` (full launch session incl. entry stop, breakpoint hit ids, register vs variable writes, memory base64 and events, disassembly offsets, source; error responses; attach without reset): 3 tests; suite 21/21. Playwright 14/14.
  - Hardware `node examples/node-ci/dap.ts --elf …/mcxa153-debug.elf --chip MCXA153 --probe mcu-link` (`DAP_RESULT=PASS`, 7.0 s incl. flashing), messages only: breakpoint verified at main.rs:40 (0x992); configurationDone ran to it (`hitBreakpointIds: [1]`); stack `step_b:40 ← step_a:57 ← main:71`; `point = {1, 2}`; evaluate `COUNTER`; disassemble at the PC; readMemory at `TABLE` using evaluate's `memoryReference` → `1111222233334444`; stepOut left `step_b` (depth 5 → 3); instruction-granularity next moved the PC; with breakpoints cleared, continue kept running; pause → `pause` stop; disconnect → terminated.
- 2026-09-17 — **Slice 7: `apps/workbench` PASS** (Playwright with the fake debugger; hardware in Chrome on the FRDM-MCXA153 over WebSocket).
  - The app: dockview 8.3 layout (Run, Source, Call stack, Variables, Peripherals, Registers, Console, Breakpoints, Disassembly, Memory) saved to localStorage and restorable ("Reset layout"); a Monaco 0.56 source view (light DOM, `monaco-editor/editor` + `editor.worker?worker`, Rust/C syntax) with the PC line and arrow, and breakpoint glyphs toggled from the gutter; an xterm debug console for DAP `output` events. The source view is driven only through DAP: an in-page `DapClient` talks to `ProbeDebugAdapter`, whose `debugger` (new public getter) also feeds the component panels. Toolbar: serve URL/token, probe, chip, ELF (File System Access; watched: a rebuild pauses, re-flashes, reloads debug info — breakpoints re-applied — and restarts), SVD, source folder (`DirectorySourceProvider`; in development `/@fs/` serves DWARF paths), stop on entry, Launch/Attach/Stop. Served standalone (port 5175) and from the flasher's origin at `/workbench/` (the dev-server plugin now serves `/inspect/` and `/workbench/`).
  - Playwright `tests/workbench.spec.ts` (2 tests): launch → stop → source view on `/build/fw/src/main.rs:40` with the PC decoration, call stack and variables panels following; gutter add/remove; removing from the gutter a breakpoint that was requested as `src/main.rs` in the Breakpoints panel; Step over from the Run panel moving the PC line; console output; layout saved, restored after reload and reset. Suite 16/16; vitest 21/21.
  - Hardware (`/workbench/?fresh=1&auto=1&token=spike&probe=mcu&chip=MCXA153&elf=/firmware/mcxa153-debug.elf&bp=40&bp2=43`, `WORKBENCH_RESULT=PASS`, 12.8 s incl. flashing): breakpoint verified; the source view opened the real `cm33-debug/src/main.rs` (through `/@fs/`) with the PC at line 40 and its glyph; call stack selected `step_b`; variables showed `point`; a gutter breakpoint at 43 verified; Continue in the Run panel stopped at 43; removing line 40 from the gutter worked; the console showed attach/flash/launch output.
  - Bug found and fixed on hardware: gutter toggles only looked at breakpoints requested under the file's absolute path, so one requested as `src/main.rs` was duplicated instead of removed. Toggles now match breakpoints by resolved file and update every request path that has one.
  - Not verified on hardware: the ELF watch → re-flash → restart path (needs a user to pick the file in Chrome's picker; it reuses Phase 2's verified watcher). Cosmetic: the Run panel's initial height is larger than intended in short windows.
  - Seen again: when a page is reloaded while `probe-rs serve` is stuck retrying failing AP writes (after a step ran away during a reload-triggered duplicate run), serve does not finish closing that WebSocket and keeps the probe ("probe is in use") until restarted.
- 2026-09-17 — **Slice 8 (part 1): `examples/monaco-ide` PASS; WebUSB capability check; WebSocket close bug fixed.** Hardware matrix pending the other two boards.
  - `examples/monaco-ide` (served at `/monaco-ide/` and standalone on 5176): ~200 lines, Monaco + xterm, talking to `ProbeDebugAdapter` only through DAP messages with its default `probe-rs serve` connection. Gutter breakpoints, run control buttons, the stopped frame's source with the PC line, and the stack and locals printed at each stop. Hardware (`MONACO_IDE_RESULT=PASS`, MCXA153): stopped at `main.rs:40` with the source open, printed `point = { x: 1, y: 2 }` from DAP variables, step over moved to line 41, disconnect terminated the session.
  - `session.debugger()` now throws `kind: 'unsupported'` (listing the missing endpoints and pointing to `probe-rs serve`) on connections without the debug endpoints, i.e. the WebUSB worker. Playwright (fake WebUSB worker): PASS; suite 17/17.
  - Bug fixed: `Client.close()` on the WebSocket transport freed the wasm client but never closed the browser WebSocket, so a page that stopped debugging but stayed open kept its `probe-rs serve` session and the probe ("probe is in use" for the next page). Node scripts never showed it because the process exits. `ProbeWebClient` now keeps the socket, closes it in `close()` and on drop; verified: after the IDE page's session ended, serve had no open connection while the page stayed open. This was the real cause of the "probe is in use" episodes logged in slices 5 and 7.
  - `examples/node-ci/debug-basic.ts`: run control without an ELF (pause, registers, memory, instruction step, disassembly, continue) for boards without debug-test firmware; MCXA153: PASS.
- 2026-09-17 — **Phase 3 hardware matrix, nRF9160 (Thingy:91, J-Link, SWD): PASS**, firmware `nrf9160-debug.elf` (same `cm33-debug` source).
  - `debug.ts` (`Debugger`, slices 1–3): all 41 checks, `DEBUG_RESULT=PASS` in 14.8 s. Differences from the MCXA153: 8 hardware comparators (8/10 breakpoints verified), CPUID 0x410FD212, SP near the top of 256 KiB RAM.
  - `dap.ts` (DAP only): 13 checks, `DAP_RESULT=PASS` in 7.5 s.
  - Workbench in Chrome (`/workbench/?fresh=1&auto=1&…&probe=j-link&chip=nRF9160_xxAA&elf=/firmware/nrf9160-debug.elf`): 9 checks, `WORKBENCH_RESULT=PASS`.
- 2026-09-17 — **Phase 3 hardware matrix, ESP32-S3 (built-in USB-JTAG, JTAG, Xtensa): PASS** without an ELF (no Xtensa Rust toolchain here), `node examples/node-ci/debug-basic.ts --chip esp32s3 --protocol Jtag --probe jtag`: pause at 0x40049b23, 34 registers (Xtensa table; PC matches the stop), memory read, instruction step, continue. `core/disassemble` answers "Request not implemented" for Xtensa on `probe-rs serve`.
- 2026-09-17 — **Phase 3 closed** (first pass; superseded by "Phase 3 close-out" at the end of this file after the follow-ups). Debugging over WebSocket to `probe-rs serve`:

  | Board (probe) | `Debugger` (41 checks) | DAP (13) | Workbench in Chrome (9) | Components page | Monaco IDE example | No-ELF run control |
  |---|---|---|---|---|---|---|
  | FRDM-MCXA153 (MCU-Link, CMSIS-DAP) | PASS | PASS | PASS | PASS (slices 4–5) | PASS | PASS |
  | Thingy:91 nRF9160 (J-Link) | PASS | PASS | PASS | — | — | — |
  | ESP32-S3 (USB-JTAG, Xtensa) | — (ARM-specific checks) | 13/13 after follow-up 2 (11/13 before) | PASS | — | — | PASS (no disassembly) |

  Hardware-free: vitest 21/21, Playwright 17/17. WebUSB transport: debugging refused with `kind: 'unsupported'` (the worker has no debug endpoints; Phase 4).
  Known gaps: ELF watch → re-flash → restart in the workbench not exercised on hardware (needs a user file pick); RTT output is not shown while debugging (the `monitor` endpoint and the status poller must not both drive a core; needs `rtt/poll_up` polling in the debugger); the workbench Run panel starts taller than intended in short windows; statement stepping in optimised code follows probe-rs semantics (step out from some lines skips a caller; locals of unwound frames may be stale or not writable).
- 2026-09-17 — **ESP32-S3 debugging with real firmware** (after the user pointed to esp-rs): `espup install --targets esp32s3` (Xtensa Rust `esp` toolchain) and `esp-generate --chip esp32s3 --headless -o probe-rs -o panic-rtt-target` scaffolded `hardware-tests/firmware/esp32s3-debug` (esp-hal 1.1, esp-bootloader-esp-idf 0.5, RTT), with the same `step_a`/`step_b`/`COUNTER`/`TABLE` program as `cm33-debug` (opt-level 1, full DWARF, no LTO). Build: `. ~/export-esp.sh && cargo build --release`.
  - Flashing: ESP32 images need the IDF format (bootloader + partition table), so the DAP adapter and the workbench now flash with `format: 'target'` (the chip's default: IDF on ESP32, ELF elsewhere); the workbench gained a protocol selector (SWD/JTAG); `dap.ts` gained `--protocol`, `--format`, `--srcPath`, `--noDisassembly`.
  - `dap.ts` on the ESP32-S3 (`--chip esp32s3 --probe jtag --protocol Jtag --srcPath bin/main.rs --noDisassembly`): 11 of 13 PASS — breakpoint verified at `bin/main.rs:39` and hit, `point = {1, 2}`, evaluate `COUNTER`, readMemory at `TABLE` (0x3c000120, `1111222233334444`), instruction step, continue, pause, disconnect; disassembly skipped (`core/disassemble` not implemented for Xtensa). **FAIL: the stack skips `step_a`** and consequently **step out does not leave `step_b`**.
  - Cause (probe-rs, not probe-web): `step_a` does call `step_b` with `callx8` (windowed ABI); at the breakpoint `a0 = 0x82010a9d`, i.e. return address 0x42010a9d in `step_a` with the window size in the top two bits. probe-rs master's Xtensa unwinder drops that caller: every stop loses exactly one frame (`step_b ← main`, and from inside `step_a`, `step_a ← Reset`). An upstream probe-rs issue/PR candidate.
  - Workbench in Chrome (`/workbench/?fresh=1&auto=1&token=spike&probe=jtag&chip=esp32s3&protocol=Jtag&elf=/firmware/esp32s3-debug.elf&srcPath=bin/main.rs&bp=39&bp2=42`): 9/9 PASS (flashed in 4.9 s; source view at `bin/main.rs:39` with the PC and glyph; gutter breakpoint at 42 hit via Run → Continue; removal from the gutter; call stack selects `step_b`, variables show `point`). Playwright 17/17, vitest 21/21 after the changes.


## Phase 3 follow-ups

Checklist (one per loop tick; hardware over WebSocket to `probe-rs serve`):
- [x] 1. RTT and semihosting output while debugging: `Debugger` polls RTT up channels (`rtt/poll_up`) and handles semihosting halts in its status poller; DAP `output` events; workbench console shows them.
- [x] 2. Xtensa unwinding in probe-rs (`prs-wasm-rpc`, probe-rs-debug): recover the caller of windowed calls (`callx8` return addresses carry the window size in the top two bits); verify stack + step out on the ESP32-S3.
- [x] 3. `probe-rs serve` robustness (`prs-wasm-rpc`): answer an error instead of panicking the connection on a stale object key or a bad core index.
- [x] 4. `probe-rs serve` monitor: an RTT client whose control block is not found yet must not rescan all of RAM on every poll (starved semihosting).
- [x] 5. Workbench: persist the picked ELF, SVD and source folder handles (IndexedDB) and offer to resume them.
- [x] 6. Memory view: lock view (do not follow refreshes) and variable highlights (ranges of watched variables).
- [x] 7. Workbench Run panel initial height.
- [x] 8. Workbench ELF watch → re-flash → restart on hardware (needs the user to pick the ELF once).
- [x] 9. Docs: hardware-tests/README.md for the debug firmware (cm33-debug, esp32s3-debug with espup) and the Node/DAP/workbench hardware checks.
- 2026-09-17 — **Follow-up 1: RTT (and semihosting) output while debugging — RTT PASS on the ESP32-S3; semihosting unit-tested, hardware check pending the Thingy:91.**
  - `probe-web-core`: `rttChannels()`, `pollRtt(channels)` (one `rtt/poll_up`, decoded with the same text/defmt/bytes decoders as the monitor) and core `handleSemihosting()`.
  - `Debugger.enableRtt({ elf })` creates an RTT client (exact scan region from `_SEGGER_RTT`); the status poller reads the up channels while the core runs and once after each stop, so no `monitor` call is needed (they could not both drive the core). A halt on a semihosting call is serviced with `core/handle_semihosting` and reported as output, not as a stop; an exit is reported as the stop. Both arrive as `output` events (`{source: 'rtt' | 'semihosting', text}`).
  - `@probe-web/dap` forwards them as `output` events (category `stdout`); `connectProbeRs` enables RTT when the program links it (`rtt: false` to opt out). The workbench enables RTT on launch and prints target output in the console in normal colour.
  - vitest: RTT attaching on a later poll, polling, and a serviced semihosting halt producing output without a stop (22/22). Playwright 17/17.
  - Hardware, ESP32-S3 via `dap.ts`: while running with no breakpoints, `n=1 result=10 | n=2 result=20 | n=3 result=27` arrived as DAP output events.
- 2026-09-17 — **Follow-up 2: Xtensa unwinding and step out (probe-rs-debug in `prs-wasm-rpc`) — PASS on the ESP32-S3, DAP 13/13 (was 11/13).**
  - Cause 1 (stack lost the caller): in `DebugInfo::unwind_impl` the caller's PC was computed from the *unwound* return address. On Xtensa the unwound a0 comes from the window spill area and is the caller's own return address, so every unwind skipped one frame. Fix: on Xtensa, take the return address from the callee's registers (the no-debug-info path already did this).
  - Cause 2 (step out stayed in `step_b`): `SteppingMode::step` used raw a0 (`0x82010a9d`, window size in the top two bits) as the return address, which has no line info. Fix: `read_return_address` replaces the top two bits with the PC's on Xtensa.
  - `cargo test -p probe-rs-debug` 98/98; the `async_esp32s3` snapshot was updated and is now more complete (`SyncExecutor::poll::{closure#0}`, `RunQueue::dequeue_all`, `SyncExecutor::poll`, `Executor::poll` appear between the task and `Executor::run_inner`; the duplicate `Reset` frame is gone). The other Xtensa snapshots (panic, coredump) are unchanged. ARM and RISC-V code paths are untouched (both changes are gated on `InstructionSet::Xtensa`).
  - Hardware, ESP32-S3 `dap.ts`: stack `step_b:39 <- step_a:56 <- __xtensa_lx_rt_main:68`; stepOut → `step_a:57` (depth 4 → 3). Worth offering upstream.
- 2026-09-17 — **Follow-up 3: `probe-rs serve` answers errors instead of panicking on unknown handles and bad core indexes — PASS (unit test + ESP32-S3).**
  - Cause: `ObjectStorage::cell` did `storage.get(&key).unwrap()`, so any request carrying a session, RTT client, flash loader or temp-file key the connection had not issued (stale, or from another connection) panicked the connection task. `memory/write*` also did `session.core(i).unwrap()`, so a bad core index panicked; the other core endpoints already returned `Core N does not exist`.
  - Fix (`prs-wasm-rpc`, probe-rs-tools `rpc/`): `cell`, `ConnectionState::object_mut[_blocking]`, `SessionState::session_blocking` and `RpcContext::session` return `Result<_, RpcError>` (`Unknown <Type> handle 0x…`); every handler propagates it with `?` (62 call sites, including the monitor/test run loops); `write_memory` uses `lift(session.core(..))?`. `probe-rs-rpc`: `impl std::error::Error for RpcError` so it converts into `anyhow`.
  - Test: `rpc::client_tests::unknown_session_key_is_an_error_not_a_panic` (a key never issued → `Unknown Session handle` for `memory/read32` and then `core/halt` on the same connection). probe-rs-tools bin tests 160/161; the one failure, `local_server_schema_matches_the_client` (`IncompatibleServer`), is not caused by this change (no wire types changed; only an `Error` impl was added), but I did not confirm it against a clean build; left for later.
  - Hardware, ESP32-S3 over WebSocket (`examples/node-ci/robustness.ts`): `memory/read32` and `memory/write8` on core 9 → `Core 9 does not exist.`; afterwards core 0 memory and `target/metadata` still answer on the same connection. `ROBUSTNESS_RESULT=PASS`.
  - The SDK's own guards (Debugger epochs, core-index checks) stay: they give better errors, and older servers still panic.
- 2026-09-17 — **Follow-up 4: paced RTT control block scans in `probe-rs serve` — PASS (unit test + ESP32-S3); plus a monitor fix found while verifying (parked secondary cores).**
  - Cause: `RttClient::try_attach` ran `Rtt::find_control_block` on every run-loop poll (every ~100 ms) until it found a block. With the default `Ram` region on firmware without RTT, each scan reads all of RAM, so the run loop spent nearly all its time scanning and answered semihosting halts late (seen on the nRF9160 in Phase 2).
  - Fix (`util/rtt/client.rs`): `ScanBackoff` — after a scan finds nothing, the next one waits twice the previous delay (100 ms → 1 s) and at least 4× as long as the scan took, so scanning uses at most ~20 % of the time; reset once a block is found. Only region scans are paced; an `Exact` address (one read) is still probed every poll.
  - Found while verifying: the monitor on the ESP32-S3 always ended at once with `Halted(Breakpoint(Software))`. The run loop also watches secondary cores and stopped on any halt, and core 1 (APP CPU, not started by the firmware) is parked at a ROM `break` at `0x400003c0`. Fix (`rpc/utils/run_loop.rs`): a secondary core that is already halted the first time it is observed is left alone until it runs; later halts are reported as before. (`probe-rs run` on master shares this run loop.)
  - Tests: `scan_backoff_doubles_up_to_a_second_and_scales_with_scan_time`; probe-rs-tools bin tests 161/162 (only the older `local_server_schema_matches_the_client` failure, see follow-up 3).
  - Hardware, ESP32-S3 (`examples/node-ci/rtt-scan-pacing.ts`, serve with `--log-file`): monitor for 8 s with an RTT client whose region (1 KiB at 0x3fcb0000) holds no block → runs the full 8 s and exits `UserExit` (before the core-1 fix: `Halted(Breakpoint(Software))` right after the first poll); scans at +0 / +0.63 / +0.93 / +1.16 / +1.17 / +1.13 / +1.16 / +1.16 s instead of every poll; log shows `Core 1 is halted at start; not watching it until it runs`. A region containing `_SEGGER_RTT` (0x3fc8a000 + 8 KiB) is still discovered. DAP regression 13/13.
  - The semihosting side (firmware without RTT on the Thingy:91) is still to be checked on hardware, together with follow-up 1.
- 2026-09-17 — **Follow-up 5: the workbench remembers the ELF, SVD and source folder — PASS (vitest, Playwright, Chrome 152); the permission prompt after a reload is checked with the user in follow-up 8.**
  - `@probe-web/artifacts`: `readPermission(handle, { request })` (check only, or ask inside a user gesture), `recallRawHandle` (any handle, directories included), a `HandleStore` interface with `indexedDbHandleStore`, and `restoreHandles(store, entries, { request })` → `{ restored, waiting, failed }`.
  - Workbench: picking the ELF, SVD (now through the File System Access picker when available, `<input type=file>` otherwise) or source folder stores its handle; on load the handles are reopened silently when the browser still grants read access, otherwise a **Reopen fw.elf, cm.svd, proj/** button asks for access (directory first). **Forget files** clears them; `?fresh=1` clears them too, `?auto=1` skips restoring. The toolbar shows the SVD and folder names.
  - vitest 29/29 (`readPermission` and `restoreHandles`: granted, waiting, requested granted/denied, a moved file reported without stopping). Playwright 18/18: picking with OPFS handles stubbed into the pickers stores `workbench.elf`, `workbench.sources`, `workbench.svd`; the folder's `src/main.rs` is served to the source view; Forget empties the store.
  - Test limitation: reading a `FileSystemHandle` back from IndexedDB crashes Playwright's Chromium 1243 (headless shell and full build), so the reload path is not in Playwright. In the user's Chrome 152 on the dev origin, an OPFS handle stored in IndexedDB reads back with `granted` in the same page and after a reload (test data removed afterwards).
- 2026-09-17 — **Follow-up 6: memory view lock and variable highlights — PASS (vitest, Playwright, ESP32-S3 workbench).**
  - `<probe-memory-view>`: **lock view** (`locked`, reflected attribute) freezes the view: stops and `refresh()` do not re-read, `goTo()` from other panels returns `false`, the address field and Refresh are disabled. **Watch variable** (`watch(expression, frameId?)`, or the "watch variable" field) evaluates the name, takes its `memoryReference` and sizes it from the type; cells in the range get a colour per variable (`data-highlight`, title = name), chips list the watched variables with ×, grouped cells are coloured when any of their bytes is covered; `highlights` can also be set directly (`MemoryHighlight {label, address, size}`).
  - `typeSize(type, pointerBytes)` (exported from `@probe-web/ui`): Rust and C primitives, `[T; N]` (nested), references and raw pointers (fat for slices/`str`/`dyn`), `usize`; `null` for aggregates, where the highlight marks the first byte and the chip shows `(?)`. Type strings checked on the ESP32-S3 first: `COUNTER` → `u32` @ 0x3fc8a794, `TABLE` → `[u16; 4]` @ 0x3c000120, locals like `&esp32s3::systimer::RegisterBlock`.
  - vitest 31/31 (`typeSize`). Playwright 19/19: `COUNTER` colours 4 cells (2 when grouped as u16), an unknown name reports "has no address", unwatch removes the colour; locked, `goTo` returns false and a stop does not call `readMemory` (0 reads), unlocked the same stop re-reads it.
  - Hardware, ESP32-S3 in Chrome (`/workbench/?fresh=1&auto=1&token=spike&probe=jtag&chip=esp32s3&protocol=Jtag&elf=/firmware/esp32s3-debug.elf&srcPath=bin/main.rs&bp=39&bp2=42`): `WORKBENCH_RESULT=PASS`, 11/11 (two new checks: the memory panel watches `TABLE` → 8 highlighted bytes `11 11 22 22 33 33 44 44` at 0x3c000120; locked, `goTo` is refused and the address stays).
- 2026-09-17 — **Follow-up 7: workbench panel sizes — PASS (Playwright, user's Chrome).**
  - Cause, measured in the user's Chrome (1470×675): the saved layout had been written while the dock measured 100×100 (automation runs the workbench in background tabs), and `fromJSON` kept that size, so Run, Source and Console each got ~100 px. Separately, the Run controls wrap to 127 px in narrow panels while the default layout fixed the group at 96 px, and the fixed sizes ran from a 100 ms timer.
  - Fix (`apps/workbench/src/main.ts`): layouts are not saved while the tab is hidden or the dock is under 200 px, and a saved layout under 200 px is ignored; after restoring, dockview is laid out to the dock element's size once it has one. The default layout waits for a real dock size, then fits the Run group to its tab header plus the controls' height (72–220 px), and sizes Console (≤ 260 px, 30 %) and Call stack (≤ 520 px, 40 %) proportionally.
  - Playwright 22/22 (3 new): at 1400×900 and 900×560 the controls fit inside the Run group with < 24 px spare and the source keeps > 30 % of the dock; a 100×100 saved layout is ignored (source > 250 px); a good saved layout restores into a smaller window and stays inside the dock.
  - User's Chrome, same tab with the 100×100 layout still stored: Run 108 px (controls fit, 3 px spare), Source 278 px, Console 228 px.
- 2026-09-17 — **Follow-up 9: debugging docs — done.** `hardware-tests/README.md` has a "Debugging (Phase 3)" section: which `probe-rs serve` to run; the two debug-target firmwares (the shared program and its exact values; build commands for `cm33-debug` per board and for `esp32s3-debug`, the `espup` toolchain setup and `~/export-esp.sh`, where to copy the ELFs; the breakpoint lines 40 / 39); per-board command lines for `debug.ts`, `dap.ts`, `debug-basic.ts`, `robustness.ts`, `rtt-scan-pacing.ts`; the browser checks (workbench for Cortex-M and ESP32-S3, components page, Monaco IDE) with their result markers; hardware-free suites; what is not automated (ELF watch, Reopen). `examples/node-ci/README.md` links to it. Checked against the sources: all three build commands run and the built ELFs are byte-identical to the copies in `apps/flash/public/firmware/`; URL parameters, line numbers and RTT output match the code.
- 2026-09-17 — **Follow-up 8: workbench ELF watch → re-flash → restart on the ESP32-S3 — PASS after two fixes; Reopen after reload — PASS.** The user picked `apps/flash/public/firmware/esp32s3-debug.elf` in Chrome; the firmware's banner was changed, rebuilt, and the ELF copied over the watched file.
  - First run: the rebuild was detected, re-flashed and restarted, and the new banner appeared, but **a breakpoint at `bin/main.rs:42` stayed "verified" while the firmware ran past it**. Cause: `reflash()` re-applied breakpoints with the new debug info and then called `resetAndHalt`, and the ESP32-S3 (like the MCX family) clears its hardware comparators on reset. Fix in the SDK: `Debugger.resetAndHalt()` re-arms tracked breakpoints after the reset; `Debugger.reset()` with breakpoints halts through the reset, re-arms, then runs, so the firmware cannot pass one first; clearing before re-arming is best effort. vitest: new test (comparators lost on reset → both breakpoints set again; reset order `resetAndHalt … set … run`; plain `reset` without breakpoints) 32/32.
  - Second fix, found on the way: the page reloaded (Vite, after the SDK edit) at an address that still had `?fresh=1`, which cleared the remembered ELF. The workbench now drops `fresh` from the address after applying it (Playwright asserts it; 22/22).
  - Second run (user picked the file again): launch → running with RTT → breakpoint at 42 → stop; rebuild with banner "(rebuilt 2)" → `esp32s3-debug.elf changed: re-flashing` → `re-flashed and restarted` → **stopped at line 42 again on the first call (`COUNTER` = 0)**, continue → stopped at 42 on the next call; the "(rebuilt 2)" banner was printed. Source and ELF restored afterwards.
  - Reopen: after a reload the toolbar showed **Reopen esp32s3-debug.elf** (Chrome drops read access to picked files on reload); the user clicked it and allowed access → the ELF was loaded (permission `granted`, 5.8 MiB, ELF magic), and Launch flashed it (3.9 s) and ran the original firmware with RTT output.
- 2026-09-17 — **Clean builds.** `prs-wasm-rpc`: the committed branch (7b8a5869, a separate worktree) and the working tree, each from an empty target directory (`--target-dir`; with `CARGO_TARGET_DIR` exported, 7 `util::cargo` tests fail on both because the cargo they spawn inherits it). Committed branch: probe-rs-tools 159/160, the failure is `local_server_schema_matches_the_client` — **it predates this work**. Working tree: probe-rs-tools 161/162 (same single failure), probe-rs-debug 98 + 5, probe-rs-rpc 6, probe-rs-rpc-client 6, all pass.
  - probe-web: `cargo clean` (3.1 GiB) → `scripts/build-wasm.sh` from an empty target in 2 min 28 s (both wasm crates, the fake-probe worker, `wire.ts` regenerated identical to HEAD) → `npm run build` OK → `tsc --noEmit` 0 errors in all 6 packages, apps/flash, apps/inspect, apps/workbench, examples/monaco-ide → vitest 32/32 → Playwright 22/22.
- 2026-09-17 — **Semihosting on the Thingy:91 (nRF9160, J-Link), closing follow-ups 1 and 4 — PASS after a serve fix.** `examples/node-ci/semihosting.ts` with `nrf9160-semihosting.elf` (banner, three stdout lines, one stderr line, `SYS_EXIT` success).
  - `--mode debugger` first **FAILED**: no output within 20 s. `core/handle_semihosting` answered `No debug state for session`: the endpoint only looked up an existing per-session debug state, which is created by `load_debug_info` or the variables endpoints, so a session that debugs firmware without loading an ELF could not service semihosting at all. The Debugger's poller reported it only as an `error` event, which nothing listened to.
  - Fix (`prs-wasm-rpc`, `rpc/functions/core_ops.rs`): create the session's debug state on first use (`entry().or_default()`), as `with_server_debug_state` already does; the session key is validated first, so no state is created for unknown sessions. Fix (`@probe-web/dap`): Debugger poller errors become DAP `output` events (category `stderr`, `probe-rs: <message>`), once per distinct message until the next stop/continue; vitest 33/33 (new adapter test).
  - After the fix: `--mode debugger` PASS — all lines arrived as `output` events 0.5–2.6 s after continue (serviced halts are not reported as stops), the exit stopped with `Breakpoint(Semihosting(ExitSuccess))`. `--mode monitor --scan ram` PASS — every line arrived and the monitor ended with `SemihostingExit(Ok)` (Phase 2: nothing within 17 s); `--mode monitor --scan none` PASS as the baseline.
  - Remaining cost: with the RAM-scanning RTT client the first line arrived ~11 s after flashing (baseline 0.2 s), i.e. one full scan of the nRF9160's RAM by the debug-profile serve; the backoff then waits ≥ 4× that before the next scan, so the rest follows within 1.6 s. Scanning in slices per poll would remove the first delay (an upstream design change, not done). The SDK still avoids the case: `<probe-rtt-terminal>` and `enableRtt` create no RTT client for ELFs without `_SEGGER_RTT`, and use an exact address when it exists.
  - probe-rs-tools bin tests 161/162 (the pre-existing schema failure only).
- 2026-09-17 — **Schema test fixed; nRF9160 regressions.**
  - `local_server_schema_matches_the_client` (failing since the wasm client patch 7e5e1029; it passes on master 86acc1b7): the in-process server sent 68 endpoints, 1 + 6 topics and 285 types, the client received 18 types and nothing else → `LostData` → `IncompatibleServer`. `get_schema_report` sized the subscription for endpoints and topics only; upstream drained it from a spawned task, the patched client collects in the same task (`tokio::join!`), and on wasm a full subscription drops at once (`subscriber_timeout_if_full` = 0). Fix (`probe-rs-rpc-client`): count the types in the subscription capacity. probe-rs-tools bin tests **162/162**, probe-rs-rpc-client 6/6; wasm rebuilt.
  - nRF9160 `debug.ts`: 41/41 PASS (also exercises reset re-arming breakpoints). `dap.ts` first **FAILED** the RTT check (no lines in 2.5 s; it had only run on the ESP32-S3): after flashing, RAM holds no control block, `rtt/channels` answered an empty list, and `Debugger.pollRtt` cached it for good. Fix: an empty channel list is not remembered, the next poll asks again (vitest: error → empty list → channels, 33/33). After the fix `dap.ts` 13/13 PASS (`n=7 result=70 | n=8 result=80 | n=9 result=81`). Playwright 22/22.
  - nRF9160 workbench in Chrome (`/workbench/?fresh=1&auto=1&token=spike&probe=j-link&chip=nRF9160_xxAA&protocol=Swd&elf=/firmware/nrf9160-debug.elf&bp=40&bp2=43`): 11/11 PASS, incl. the memory panel (`TABLE` at 0x1c98). A first attempt without `protocol=Swd` failed to attach: the workbench had remembered JTAG from the ESP32-S3 run (documented in hardware-tests/README.md).
- 2026-09-17 — **Phase 3 close-out, nRF9160 (Thingy:91, J-Link) on the final code — all PASS.** `probe-rs serve` built from the `prs-wasm-rpc` working tree with every fix above; wasm rebuilt.
  - Earlier today on this code: `debug.ts` 41/41, `dap.ts` 13/13 (incl. RTT output), workbench 11/11, `semihosting.ts --mode debugger` and `--mode monitor --scan ram|none`.
  - `robustness.ts --address 0x20000000` (new option; the ESP32-S3 RAM address was hard-coded): PASS 4/4 (`Core 8 does not exist.` for read and write, connection keeps serving). `debug-basic.ts`: PASS (pause at 0x900, 63 registers, memory, instruction step, disassembly, continue).
  - Components page `debug.html?auto=1&token=spike&probe=j-link&chip=nRF9160_xxAA&elf=/firmware/nrf9160-debug.elf&line=40&table=0x1c98&svd=/svd/cortex-m-scb.svd`: first 12/13 — the Peripherals check compared CPUID with the MCXA153's `0x411FD210`, while the nRF9160 correctly reads `0x410FD212`. The check now accepts any Arm Cortex-M33 CPUID (implementer 0x41, part 0xD21), or an exact `?cpuid=`. Rerun: **13/13, `DEBUGUI_RESULT=PASS`** (first run on this board).
  - Monaco IDE example `/monaco-ide/?auto=1&token=spike&probe=j-link&chip=nRF9160_xxAA&elf=/firmware/nrf9160-debug.elf&bp=40`: **`MONACO_IDE_RESULT=PASS`** (stopped at `main.rs:40` with the source open, `point = { x: 1, y: 2 }` from DAP variables, step over → line 41, terminated; first run on this board). Result read from a screenshot: evaluating `innerText` on that page timed out in the automation.
- 2026-09-17 — **Phase 3 close-out, FRDM-MCXA153 (MCU-Link, CMSIS-DAP) on the final code — all PASS.** Same `serve` and wasm build as the nRF9160 run.
  - Node: `debug.ts --svd …/cortex-m-scb.svd` 41/41 (`DEBUG_RESULT=PASS`), `dap.ts` 13/13 incl. RTT output (`n=4 result=40 | n=5 result=50 | n=6 result=54`), `debug-basic.ts` PASS, `robustness.ts --address 0x20000000` PASS.
  - Chrome: workbench (`probe=mcu&chip=MCXA153&protocol=Swd&elf=/firmware/mcxa153-debug.elf&bp=40&bp2=43`) 11/11 incl. the memory panel (`TABLE` at 0x1c98) and continue to the gutter breakpoint at 43 (breakpoints survive the MCX reset); components page (`…&line=40&table=0x1c98&svd=/svd/cortex-m-scb.svd`) 13/13, CPUID `0x411FD210`; Monaco IDE example `MONACO_IDE_RESULT=PASS` (stopped at `main.rs:40`, `point = { x: 1, y: 2 }`, step over → line 41, terminated; read from a screenshot).
- 2026-09-17 — **Phase 3 close-out, ESP32-S3 (built-in USB-JTAG, Xtensa) on the final code — all PASS.**
  - Node: `dap.ts` 13/13 (stack `step_b:39 <- step_a:56 <- __xtensa_lx_rt_main:68`, step out → `step_a:57`, RTT `n=1 result=10 | n=2 result=20 | n=3 result=27`), `debug-basic.ts` PASS, `robustness.ts` PASS (`Core 9 does not exist.`), `rtt-scan-pacing.ts`: a region without a control block monitors the full 8 s and exits `UserExit`; a region containing `_SEGGER_RTT` is discovered.
  - Chrome: workbench (`probe=jtag&chip=esp32s3&protocol=Jtag&elf=/firmware/esp32s3-debug.elf&srcPath=bin/main.rs&bp=39&bp2=42`) 11/11 (memory panel `TABLE` at 0x3c000120; continue stops at the gutter breakpoint at 42). ELF watch → re-flash → restart and Reopen were verified earlier today on the same `Debugger` reset code.
- 2026-09-17 — **Upstream probe-rs branches prepared (local only, not pushed).** Checked GitHub first: no open or merged probe-rs PRs/issues for these. Three worktrees off `origin/master` b74ac610 (4 commits past our base; none touch these files), each with a changelog fragment, `cargo fmt --check` and clippy clean:
  - `../prs-up-xtensa`, branch `fix/xtensa-unwind-step-out` (722001b0): probe-rs-debug unwinder + stepping, async ESP32-S3 snapshot updated; probe-rs-debug 98 + 5 tests.
  - `../prs-up-serve`, branch `fix/serve-unknown-handles` (0fa6db8e): fallible object/session lookups, `write_memory` core index, `RpcError: Error`, semihosting state on demand, new client test; probe-rs-tools 161/161 (master has no wasm patch, so the schema test passes there).
  - `../prs-up-runloop`, branch `fix/run-loop-rtt-scan-parked-cores` (8a415738): `ScanBackoff` + unit test, parked secondary cores; probe-rs-tools 161/161.
  - Not upstreamed: the schema-report capacity fix only matters with our wasm client patch (upstream collects in a spawned task), so it stays on `wasm-rpc-client`.

## Phase 3 close-out (2026-09-17)

Phase 3 is complete: debugging in the browser and from Node over WebSocket to `probe-rs serve`
(`prs-wasm-rpc` working tree), with every follow-up done and all checks re-run on the final code.

| Board (probe) | `Debugger` `debug.ts` (41) | DAP `dap.ts` (13) | Workbench (11) | Components page (13) | Monaco IDE | No-ELF run control | Semihosting | serve errors `robustness.ts` |
|---|---|---|---|---|---|---|---|---|
| FRDM-MCXA153 (MCU-Link, CMSIS-DAP) | PASS | PASS | PASS | PASS | PASS | PASS | — (no semihosting firmware) | PASS |
| Thingy:91 nRF9160 (J-Link) | PASS | PASS | PASS | PASS | PASS | PASS | PASS (debugger + monitor) | PASS |
| ESP32-S3 (USB-JTAG, Xtensa) | — (Cortex-M checks) | PASS (no disassembly) | PASS | — (Cortex-M page) | — | PASS | — | PASS |

Also verified on hardware: RTT output while debugging (all three), workbench ELF watch → re-flash →
restart with breakpoints kept and Reopen after reload (ESP32-S3), monitor RTT scan pacing and parked
secondary cores (ESP32-S3, nRF9160). Hardware-free: vitest 33/33, Playwright 22/22; clean builds of
probe-web (wasm, `npm run build`, `tsc` across packages and apps) and `prs-wasm-rpc`
(probe-rs-tools 162/162, probe-rs-debug 103, probe-rs-rpc 6, probe-rs-rpc-client 6).

Known limitations (not blocking): a monitor whose RTT client scans all of RAM waits for that first
scan (~11 s on the nRF9160) before other work; `probe-rs serve` has no Xtensa disassembly; `debug.ts`
and the components page are Cortex-M specific; the workbench remembers the last protocol, so hardware
URLs pass `protocol=`; Playwright's Chromium cannot read `FileSystemHandle`s back from IndexedDB, so
the Reopen path is checked by hand; debugging over WebUSB is Phase 4.

Committed and pushed: probe-web `b0f7f77`, `prs-wasm-rpc` branch `wasm-rpc-client` `b33b226f`
(fork). The upstream probe-rs branches (`fix/xtensa-unwind-step-out`, `fix/serve-unknown-handles`,
`fix/run-loop-rtt-scan-parked-cores`) stay local in `../prs-up-*`: upstream PRs are deferred until
some time after probe-web is public.

